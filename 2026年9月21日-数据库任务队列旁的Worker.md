将昨天[笔记](2026-09-20-FastAPI与数据库任务队列)涉及到的 Worker 的代码详细看一下。


```PYTHON
import time
import sqlite3
import asyncio

async def worker_loop():
    print("Worker 启动了，准备看登记本领任务...")
    
    while True:
        # 1. 检查数据库，找一条 status 为 'pending' 的任务
        conn = sqlite3.connect("tasks.db")
        cursor = conn.cursor()
        cursor.execute("SELECT id, image_url FROM tasks WHERE status = 'pending' LIMIT 1")
        task = cursor.fetchone()
        
        if task:
            task_id, image_url = task
            # 2. 认领任务：更新状态为 'processing' (处理中)，防止别的 Worker 抢走
            cursor.execute("UPDATE tasks SET status = 'processing' WHERE id = ?", (task_id,))
            conn.commit()
            conn.close()
            
            print(f"领到任务 {task_id}，正在调用 ComfyUI 抠图和重绘...")
            
            # 3. 模拟调用 ComfyUI 或重绘逻辑（等待 5 秒）
            # 这里用了 await asyncio.sleep，等待期间 Worker 不会卡死，还能发心跳
            await asyncio.sleep(5) 
            
            # 4. 干完活了，将结果写回数据库，状态改为 'completed'
            conn = sqlite3.connect("tasks.db")
            cursor = conn.cursor()
            cursor.execute(
                "UPDATE tasks SET status = 'completed', result_url = ? WHERE id = ?", 
                (f"http://result_{task_id}.png", task_id)
            )
            conn.commit()
            conn.close()
            print(f"任务 {task_id} 搞定！结果已存库。")
            
        else:
            conn.close()
            # 没领到任务，休息 2 秒再看下一轮
            await asyncio.sleep(2)

if __name__ == "__main__":
    asyncio.run(worker_loop())
```

**理解要点一**，可以看到这是一个用`async def`定义的协程。对于协程来说，就需要人为去指定跳出执行的点位。所以用`async def`所定义的协程函数中，会配合`await`跳出字段使用。
在这里的体现是：

```python
import asyncio
async def worker_loop():
	...
	...
	await ... #从这里开始跳出，耗时任务执行完之后，网卡将提醒cpu，将控制权拿回
	...
	await asyncio.sleep(5) #一个协程中可以有多个 await 字段
	
if __name__=='__main__':
	asyncio.run(worker_loop()) #事件循环主控

```

**理解要点二**，这里的`asyncio.run(worker_loop())`是将协程加入事件循环管理，单独使用`async def worker_loop()`定义的是一个协程，直观来感受就是静态的对象。直接调用它的话是不会运行的，只会返回一个协程对象：
```python
result = worker_loop() #直接调用一个协程对象
print(result) 
```
控制台会输出类似：
`<coroutine object worker_loop at 0x7f9a...>`
看起来只是返回了这个协程所在的内存地址。

**理解要点三**，`await`是协程专用的关键字，也就是说,`await`只能用在一个协程也就是`async def`定义的函数体内部。`await`关键字后，要接一个“可等待对象”，不是随便接什么都可以。直观理解来看，它要是一个“能承诺在未来某一个时刻会给你的答复”的对象。按更加底层的说来来解释，在 Python 底层，只要这个对象内部实现了 `__await__()` 魔术方法，它就是可等待对象。

下一步可扩展学习，ComfyUI的任务是如何被包装为一个可等待对象放在`await`之后的。

速记几个东西，这里的Worker：
	1. 是一个协程，可用`await`挂起。
	2. 是一个永久的循环，一直在查看任务告示板，做的任务就是将“状态”改为“处理中”，领取出必要的参数，将参数交给处理的函数。做完后返回结果。
	3. Worker其实也没有干什么重活。主要工作是查询数据库，查找出合适的记录，将参数记录，交给处理函数。这里也用到`await`。

